PORT = 8080

# Origem liberada no CORS. Em desenvolvimento "*" já funciona; em produção,
# restrinja para a URL exata do frontend (ex.: https://app.exemplo.com).
CORS_ORIGIN = *

# Caminho do arquivo onde as tasks são persistidas em disco.
DATA_FILE = data/tasks.json
